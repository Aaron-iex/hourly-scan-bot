import { Anchor, Cpu, Radio, Rocket, Signal, type LucideIcon } from "lucide-react";
import { Button } from "@/components/ui/button";
import { TRACKS } from "@/data/zeroth";
const ICONS = { waves: Radio, rocket: Rocket, anchor: Anchor, zap: Cpu, flame: Signal } satisfies Record<string, LucideIcon>;
export function Sectors({ onRegister }: { onRegister: (track: string) => void }) { return <section id="domains" className="mx-auto max-w-7xl px-4 py-24 sm:px-6 lg:px-8"><div className="max-w-2xl"><p className="font-mono-tech text-xs tracking-[0.28em] text-primary">// DEPARTMENT DOMAINS</p><h2 className="mt-3 font-display text-3xl font-black uppercase sm:text-5xl">Where ideas become <span className="text-accent">systems</span></h2><p className="mt-4 text-muted-foreground">Five connected pathways through the ECE discipline. Choose a domain, find your team, and make something that matters.</p></div><div className="mt-10 grid gap-4 md:grid-cols-2 lg:grid-cols-3">{TRACKS.map((track) => { const Icon = ICONS[track.icon]; return <article key={track.id} className="panel-tactical flex flex-col p-6 transition-transform hover:-translate-y-1"><div className="flex items-start justify-between"><span className="font-mono-tech text-xs tracking-[0.2em] text-primary">{track.code}</span><Icon className="text-accent" aria-hidden /></div><h3 className="mt-8 font-display text-xl font-bold uppercase">{track.title}</h3><p className="mt-3 flex-1 text-sm leading-relaxed text-muted-foreground">{track.brief}</p><div className="mt-6 flex flex-wrap gap-2">{track.stack.map((tag) => <span key={tag} className="border border-border px-2 py-1 font-mono-tech text-[10px] text-accent">{tag}</span>)}</div><Button variant="link" className="mt-5 justify-between border-t border-border pt-4" onClick={() => onRegister(track.title)}>Select domain <span aria-hidden>→</span></Button></article>; })}</div></section>; }
