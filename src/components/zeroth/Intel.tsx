import { useState } from "react";
import { ChevronDown, Cpu, ShieldCheck } from "lucide-react";
import { Button } from "@/components/ui/button";
import { FAQS, LEADERSHIP, STATS } from "@/data/zeroth";
function Person({ name, role, org, featured = false }: { name: string; role: string; org: string; featured?: boolean }) { return <article className={`panel-tactical p-5 ${featured ? 'md:col-span-2' : ''}`}><div className="flex items-start gap-4"><span className="grid size-12 shrink-0 place-items-center border border-accent/50 bg-accent/10 font-display font-black text-accent">{name.split(' ').map((w) => w[0]).slice(0,2).join('')}</span><div><h4 className="font-display text-base font-bold uppercase">{name}</h4><p className="mt-1 font-mono-tech text-[10px] tracking-[0.14em] text-primary">{role}</p><p className="mt-2 text-sm text-muted-foreground">{org}</p></div></div></article>; }
export function Intel({ preview, onExpand }: { preview?: boolean; onExpand?: () => void }) { const [open, setOpen] = useState(0); return <section id="intel" className="mx-auto max-w-7xl px-4 py-24 sm:px-6 lg:px-8"><div className="grid gap-10 lg:grid-cols-[1.2fr_0.8fr]"><div><p className="font-mono-tech text-xs tracking-[0.28em] text-primary">// ABOUT THE DEPARTMENT</p><h2 className="mt-3 font-display text-3xl font-black uppercase sm:text-5xl">Signal, systems, <span className="text-accent">impact</span></h2><p className="mt-5 text-lg leading-relaxed text-muted-foreground">The Department of Electronics and Communication Engineering nurtures engineers who connect imagination with technology. From embedded intelligence to next-generation communication, we learn by building.</p><p className="mt-4 leading-relaxed text-muted-foreground">Our department combines strong fundamentals, practical laboratories, student clubs, and a culture of collaboration to prepare students for meaningful work in a connected world.</p><div className="mt-8 flex flex-wrap gap-3"><span className="flex items-center gap-2 border border-primary/40 px-3 py-2 font-mono-tech text-xs text-primary"><ShieldCheck /> PRACTICAL LEARNING</span><span className="flex items-center gap-2 border border-accent/40 px-3 py-2 font-mono-tech text-xs text-accent"><Cpu /> INNOVATION FIRST</span></div></div><dl className="grid grid-cols-2 gap-4">{STATS.map((s) => <div key={s.label} className="panel-tactical p-5"><dt className="font-mono-tech text-[10px] tracking-[0.18em] text-muted-foreground">{s.label}</dt><dd className="mt-2 font-display text-2xl font-black text-accent">{s.value}</dd><p className="mt-1 text-xs text-muted-foreground">{s.desc}</p></div>)}</dl></div>{preview ? <div className="mt-10 text-center"><Button variant="tactical" size="xl" onClick={onExpand}>Meet the department team</Button></div> : <><div className="mt-20"><p className="font-mono-tech text-xs tracking-[0.25em] text-primary">// DEPARTMENT LEADERSHIP</p><h3 className="mt-3 font-display text-2xl font-black uppercase">Guided by <span className="text-accent">experience</span></h3><div className="mt-6 grid gap-4 md:grid-cols-2"><Person {...LEADERSHIP.hod} featured /></div><div className="mt-4 grid gap-4 md:grid-cols-2">{LEADERSHIP.faculty.map((p) => <Person key={p.name} {...p} />)}</div><h3 className="mt-12 font-display text-2xl font-black uppercase">Student <span className="text-accent">team</span></h3><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{LEADERSHIP.students.map((p) => <Person key={p.name} {...p} />)}</div></div><div className="mt-20 grid gap-8 lg:grid-cols-[0.8fr_1.2fr]"><h3 className="font-display text-2xl font-black uppercase">Common <span className="text-accent">questions</span></h3><div className="divide-y divide-border border-y border-border">{FAQS.map((f,i) => <div key={f.q}><button onClick={() => setOpen(open === i ? -1 : i)} className="flex w-full items-center justify-between gap-4 py-4 text-left" aria-expanded={open === i}><span className="font-display font-bold">{f.q}</span><ChevronDown className={open === i ? 'rotate-180 text-primary' : 'text-primary'} /></button>{open === i && <p className="pb-5 text-sm leading-relaxed text-muted-foreground">{f.a}</p>}</div>)}</div></div></>}</section>; }
